{
  "name": "@package/ts-config",
  "version": "0.0.0",
  "private": true,
  "files": [
    "node.config.json"
  ],
  "scripts": {
    "clean:turbo": "rm -rf .turbo",
    "clean:node-modules": "rm -rf node_modules"
  },
  "devDependencies": {}
}
