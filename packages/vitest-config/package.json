{
  "name": "@package/vitest-config",
  "version": "0.0.0",
  "private": true,
  "files": [
    "node.config.js"
  ],
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf .turbo"
  },
  "devDependencies": {
    "vitest": "^0.28.5"
  }
}
