{
  "name": "@package/vitest-config",
  "version": "0.0.0",
  "private": true,
  "files": [
    "node.config.js"
  ],
  "scripts": {
    "clean:turbo": "rm -rf .turbo",
    "clean:node-modules": "rm -rf node_modules"
  },
  "devDependencies": {
    "vitest": "^0.28.5"
  }
}
